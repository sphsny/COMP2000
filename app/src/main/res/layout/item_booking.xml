<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Card"
    android:id="@+id/reservation_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:layout_marginBottom="12dp">

    <!-- Date + Time -->
    <TextView
        android:id="@+id/booking_time"
        android:textSize="18sp"
        android:textStyle="bold"
        android:text="10/10/2025 - 14:00"
        android:textColor="@color/blue"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/res_edit"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginEnd="12dp"/>

    <!-- People -->
    <TextView
        android:id="@+id/booking_people"
        android:textSize="16sp"
        android:textColor="@color/black"
        android:text="Reservation for 2 people"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/res_edit"
        app:layout_constraintTop_toBottomOf="@id/booking_time"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="12dp"/>

    <!-- Edit button -->
    <ImageButton
        android:id="@+id/res_edit"
        style="@style/Icon"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_edit"
        android:contentDescription="Edit reservation"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/res_delete"
        android:padding="8dp"/>

    <!-- Delete button -->
    <ImageButton
        android:id="@+id/res_delete"
        style="@style/Icon"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_delete"
        android:contentDescription="Delete reservation"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:padding="8dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>
