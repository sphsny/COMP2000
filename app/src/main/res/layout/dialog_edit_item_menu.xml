<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:fillViewport="true">


<androidx.constraintlayout.widget.ConstraintLayout
        style="@style/Card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="20dp">

        <!-- Title -->
        <TextView
            android:id="@+id/dialog_title"
            style="@style/PrimaryHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Edit Item"
            android:layout_marginBottom="16dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Image Preview -->
        <ImageView
            android:id="@+id/input_image"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginTop="10dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_menu"
            android:contentDescription="Food Image"
            app:layout_constraintTop_toBottomOf="@id/dialog_title"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Upload Button -->
        <Button
            android:id="@+id/upload_item_image_btn"
            style="@style/PrimaryButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Upload Image"
            android:layout_marginTop="10dp"
            app:layout_constraintTop_toBottomOf="@id/input_image"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Item Name -->
        <EditText
            android:id="@+id/input_item_name"
            style="@style/Form"
            android:hint="Item Name"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_marginTop="15dp"
            app:layout_constraintTop_toBottomOf="@id/upload_item_image_btn"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Details -->
        <EditText
            android:id="@+id/input_item_details"
            style="@style/Form"
            android:hint="Item Details"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:gravity="top"
            android:layout_marginTop="15dp"
            app:layout_constraintTop_toBottomOf="@id/input_item_name"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Price -->
        <EditText
            android:id="@+id/input_item_price"
            style="@style/Form"
            android:hint="Item Price"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_marginTop="15dp"
            app:layout_constraintTop_toBottomOf="@id/input_item_details"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Cancel Button -->
        <Button
            android:id="@+id/cancel_item_btn"
            style="@style/PrimaryButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cancel"
            android:layout_marginTop="20dp"
            android:backgroundTint="@color/light_grey"
            app:layout_constraintTop_toBottomOf="@id/input_item_price"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- Save Button -->
        <Button
            android:id="@+id/save_item_btn"
            style="@style/PrimaryButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Save"
            android:layout_marginTop="20dp"
            app:layout_constraintTop_toBottomOf="@id/input_item_price"
            app:layout_constraintEnd_toEndOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
